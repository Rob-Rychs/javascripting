// WORKING WITH ARRAYS:

.splice()

.valueOf()

.join()

.sort()

numbers.sort(function(x,y){return x - y});

.concat()

.pop() // removes last item of an array

.push() // adds item to an array

.shift() // removes first item of an array

.unshift() // adds a first item to an array
