function append(parent, el) {
  return parent.appendChild(el); 
}
// function append() accepts two arguments, the parent element and the element to append to the parent
// Append the second parameter(el) to the first one
