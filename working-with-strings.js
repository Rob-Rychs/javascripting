// WORKING WITH STIRNGS:

.length vs .lengthOf() // get length of string

.indexOf("") // get begining character of search string

.slice(x,y) // slices a string at index start (x) to index end (y) (y=optional)

.substr(x,y) // gets a substring by index start (x) and length of substring (y)

.replace(x,y) // replaces string x with string y

.charAt(x) // character at index x

.split() // splits a string into an array, use .split(" ") to split the words between the spaces

.trim() // trims white space of string

.toUpperCase() 

.toLowerCase() 

strToStyle.property() .big() .bold() .fontcolor("blue") .fontsize("8em") .italics() .link("url") .small() .strike() .sub() .sup()
